# Python_3D_Scanner snapshot

This repository snapshot was prepared from the local project at `D:\code\Python_3D_Scanner`.

- Local git commit: `ec7aa55` (`Initial project snapshot`)
- Snapshot archive: `snapshots/Python_3D_Scanner_snapshot_ec7aa55.zip`
- The archive contains the tracked source, docs, build spec, runtime hook, scripts, requirements, and sample OBJ used for this version.
- Generated capture folders, build outputs, IDE files, caches, and temporary files were intentionally excluded by `.gitignore`.

Note: direct `git push` from this machine failed because `github.com:443` is unreachable locally. SSH to GitHub is reachable, but no GitHub SSH key is configured on this Windows account. This snapshot was uploaded through the connected GitHub app so this version is preserved before local next-generation development continues.
